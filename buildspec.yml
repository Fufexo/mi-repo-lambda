version: 0.2

phases:
  install:
    commands:
      - npm install
  build:
    commands:
      - sam build
  post_build:
    commands:
      - sam package --s3-bucket userbucketprueba --output-template-file packaged.yaml
artifacts:
  files:
    - packaged.yaml
