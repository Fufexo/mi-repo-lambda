{
  "name": "mi-lambda-function",
  "version": "1.0.0",
  "description": "Una función Lambda para gestionar usuarios en DynamoDB",
  "main": "src/index.js",
  "scripts": {
    "build": "sam build",
    "deploy": "sam deploy --guided",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/lib-dynamodb": "^3.0.0"
  },
  "devDependencies": {
    "aws-sdk": "^2.0.0"
  },
  "author": "Fabrizio",
  "license": "MIT"
}
